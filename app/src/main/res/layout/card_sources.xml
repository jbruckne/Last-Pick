<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/movie_card_sources"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="@dimen/movie_card_top_margin"
    app:cardBackgroundColor="?attr/colorBackgroundFloating"
    app:cardCornerRadius="0dp"
    app:cardElevation="2dp"
    tools:targetApi="N">

    <LinearLayout
        android:id="@+id/movie_layout_sources"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <TextView
            android:id="@+id/movie_label_sources"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="16dp"
            android:textSize="16sp"
            android:textStyle="bold"
            android:text="@string/label_sources"/>

        <android.support.v7.widget.RecyclerView
            android:id="@+id/movie_grid_sources"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:layout_marginEnd="16dp"
            android:layout_marginBottom="16dp"
            android:nestedScrollingEnabled="false"
            android:orientation="vertical"
            app:layoutManager="StaggeredGridLayoutManager"
            app:spanCount="3"
            tools:listitem="@layout/item_source"
            tools:layout_height="120dp"/>
    </LinearLayout>
</android.support.v7.widget.CardView>